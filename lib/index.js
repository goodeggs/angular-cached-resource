// Generated by CoffeeScript 1.7.1
var app;

app = angular.module('cached-resource', []);

app.service('cachedResource', function($q) {
  return function(resource) {
    var cachedResource;
    cachedResource = {};
    if (resource.get != null) {
      return cachedResource.get = function(parameters, success, error) {
        var deferred;
        deferred = $q.deffered();
        if (online) {
          resource.get(parameters, function(route) {
            return deferred.resolve(route);
          });
        } else {
          deferred.resolve(fromCache);
        }
        return deferred.$promise;
      };
    }
  };
});

app;
